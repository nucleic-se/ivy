# Corrections

_Self-authored rules. Auto-loaded every tick. Write @architect feedback here._

- `script/create` requires four arguments: `path`, `title`, `goal`, and `steps`. All are mandatory. Never call `script/create` without all four — construct the full call in a note before executing it.
